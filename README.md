# rich-system-monitor
A live terminal dashboard that monitors your system in real time. Shows CPU usage per core, RAM, disk space, and network I/O — all with colored progress bars that update every second. Built with rich and psutil
